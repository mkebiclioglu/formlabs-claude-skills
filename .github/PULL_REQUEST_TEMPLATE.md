## What does this change?

<!-- One or two sentences. Link the issue if there is one: "Fixes #12". -->

## How was it tested?

- [ ] `python3 scripts/validate.py` passes
- [ ] Tried it with `claude --plugin-dir plugins/formlabs` (for skill changes: paste the relevant part of the transcript)

## Checklist

- [ ] Skill `description` still says *when* the skill applies, not only what it does
- [ ] Docs (`README.md`, `docs/docs.html`) updated if behaviour or install steps changed
